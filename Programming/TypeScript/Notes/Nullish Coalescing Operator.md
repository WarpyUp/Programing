Оператор `??` перевіряє чи значення не дорівнює `null` або `undefined`.

Використовується так
`let a = number ?? 0;`

Якщо потрібно перевірити чи значення не Falsy тоді використовується [[Logical OR|Logical OR]]
